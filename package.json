{
  "name": "squillo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "rm -rf dist && bun build index.html --outdir=dist",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16",
    "typescript": "^5.8.3"
  }
}
